<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%@ page import="java.sql.*"%>
<html>
	<head>
		<title>Insert title here</title>
	</head>
	<body>
		<%
			String name = request.getParameter("name");
			String id = request.getParameter("id");
			String type = request.getParameter("select");
			Class.forName("com.mysql.cj.jdbc.Driver");
			Connection conn = DriverManager.getConnection("jdbc:mysql://localhost:3306/students", "root", "123456");
			String insertSql = "INSERT INTO students_for_javaweb(name, id) VALUES (?, ?)";
			PreparedStatement preparedSql = null;
			if("增加".equals(type)) {
				preparedSql = conn.prepareStatement(insertSql);
				if("".equals(name) || "".equals(id)) {
					return;
				}
				else {
					preparedSql.setString(1, name); 
					preparedSql.setString(2, id); 
				}
				preparedSql.executeUpdate();
				preparedSql.close();
				conn.close();
			}
			else if("")

		%>
		<form action="stu_info_mng_sys.html">
			<input type="submit" value="返回">
		</form>
	</body>
</html>